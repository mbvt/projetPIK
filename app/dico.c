/* dico.c  */

# include <stdlib.h>

# include "dico.h"

// 2 types of Dico : child and adult
// 27 levels
// some levels use previous mixed up dico
// total 40 dico
// need need to do approximatively 20 regex function or just one with different
// parameters.
struct dico *init_dico(int id_lvl)
{
  struct dico *dico = malloc(sizeof(struct dico));
   
}
